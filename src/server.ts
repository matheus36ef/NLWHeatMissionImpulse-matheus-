import { serveHttp } from "./app";


//Servidor ---------------------------------------------------------------
serveHttp.listen(4000, () => {console.log(`Server is run on PORT 4000--`);
});
//------------------------------------------------------------------------
